-- Create a table if it doesnt exist yet, that must be called first_name and
-- the description must be, id - INT and name - VARCHAR(256). Show or select
-- are not allowed to use
CREATE TABLE IF NOT EXISTS first_table (
	id INT
	name VARCHAR(256));
